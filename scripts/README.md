# Scripts

Utility scripts for development and deployment.

## Planned Scripts
- `setup_dev.sh` - Set up development environment
- `flash_firmware.sh` - Flash firmware to ESP32 boards
- `seed_database.py` - Populate test data
- `generate_test_data.py` - Generate synthetic sensor data

These will be added as needed during development.
